# Copie para .env e preencha. NUNCA commite o .env.
# Este arquivo contém apenas NOMES de variáveis — nenhum valor real.

# ─── Telegram ────────────────────────────────────────────────────────────────
# Criado no @BotFather. Formato: 123456789:AA...
TELEGRAM_BOT_TOKEN=

# ─── Gateway de modelo — Estágio 1 (extração do edital) ──────────────────────
# provider: anthropic | openai
LLM_EXTRACTION_PROVIDER=anthropic
LLM_EXTRACTION_MODEL=claude-opus-5
LLM_EXTRACTION_BASE_URL=https://api.anthropic.com
LLM_EXTRACTION_API_KEY=
# Quanto de estrutura o provedor consegue impor por conta própria:
#   schema — decodificação restrita com o JSON Schema completo (Anthropic)
#   json   — só garante JSON sintático (DeepSeek: json_schema é recusado)
#   none   — nada; o schema vai no texto do prompt (Ollama)
# Em todos os casos quem garante o contrato é a validação posterior.
LLM_EXTRACTION_STRUCTURED=schema
# Orçamento de saída. Modelo de raciocínio consome boa parte dele pensando —
# o deepseek-v4-flash usou 24 mil tokens de reasoning num edital de 4 páginas.
LLM_EXTRACTION_MAX_TOKENS=16000
# Raciocinio do modelo: `none` desliga. Ver docs/evidence/ para o efeito.
LLM_EXTRACTION_REASONING=

# ─── Gateway de modelo — Estágio 3 (Q&A sobre a ficha) ───────────────────────
# Ollama expõe uma API compatível com OpenAI em /v1
LLM_QA_PROVIDER=openai
LLM_QA_MODEL=qwen3:14b
LLM_QA_BASE_URL=http://host.docker.internal:11434
LLM_QA_API_KEY=ollama

# ─── Teto de custo (slide 8) ─────────────────────────────────────────────────
# O gateway recusa a chamada quando o acumulado ultrapassa este valor.
BUDGET_USD_LIMIT=5

# ─── DataJud (API pública do CNJ) ────────────────────────────────────────────
# Chave PÚBLICA, publicada pelo CNJ. Não é segredo, mas fica fora do git
# porque o CNJ avisa que pode trocá-la a qualquer momento — buscar a atual em
# http://datajud-wiki.cnj.jus.br/api-publica/acesso/
DATAJUD_API_KEY=
DATAJUD_BASE_URL=https://api-publica.datajud.cnj.jus.br

# ─── Serviços internos ───────────────────────────────────────────────────────
DOCLING_URL=http://docling:5001

POSTGRES_USER=leilao
POSTGRES_PASSWORD=
POSTGRES_DB=leilao

# ─── n8n ─────────────────────────────────────────────────────────────────────
# Chave de 32+ caracteres usada para criptografar credenciais no banco do n8n.
N8N_ENCRYPTION_KEY=
GENERIC_TIMEZONE=America/Sao_Paulo

# ─── Túnel público (preenchido por ./scripts/tunnel-url.sh) ──────────────────
# URL HTTPS que o Telegram usa para entregar os webhooks.
WEBHOOK_URL=
